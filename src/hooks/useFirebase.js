import React from 'react';
import firebaseInit from '../Firebase/firebase.init';

firebaseInit();

const useFirebase = () => {
    return (
        <div>
            
        </div>
    );
};

export default useFirebase;